yt-dlp[default]==2026.8.19
SpotipyFree==1.9.14
# Dependência importada pelo spotapi, mas não declarada no wheel do SpotipyFree.
redis==8.1.0
boto3==1.43.100
supabase==2.31.0
# Plugin de PO token (só age se POT_PROVIDER_BASE_URL estiver setado + provedor no ar).
bgutil-ytdlp-pot-provider==2.0.0
